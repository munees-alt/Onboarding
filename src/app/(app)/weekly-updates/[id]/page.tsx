import { notFound } from "next/navigation";

// Archived (platform cleanup, 2026-07) — Weekly Client Updates is no longer reachable.
export default async function WeeklyUpdateDetailPage() {
  notFound();
}
